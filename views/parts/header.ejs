<style>
    #pages_header{
        height: 60px; 
    }
</style>
<!--<nav class="navbar navbar-default" role="navigation" ng-controller="NavController">
  <div class="container-fluid">
     Brand and toggle get grouped for better mobile display 
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" ng-click="isCollapsed = !isCollapsed">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Angular messages</a>
    </div>

     Collect the nav links, forms, and other content for toggling 
    <div class="collapse navbar-collapse" collapse="isCollapsed">
      <ul class="nav navbar-nav">
        <li ng-show="auth.isAuthenticated()"><a href="#/messages">Messages</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li ng-hide="auth.isAuthenticated()"><a href="#/login">Login</a></li>
        <li ng-hide="auth.isAuthenticated()"><a href="#/register">Register</a></li>
        <li ng-show="auth.isAuthenticated()"><p class="navbar-text">Hello, <strong>{{user().email}}</strong></p></li>
        <li ng-show="auth.isAuthenticated()"><a href="#" ng-click="logout()">Logout</a></li>
      </ul>
    </div> /.navbar-collapse 
  </div> /.container-fluid 
</nav>-->




<header class="headers" id="pages_header">
    <nav class="navbar navbar-inverse" role="navigation" ng-controller="NavController"> 
        <div class="navbar-header">

            <button type="button" ng-click="isCollapsed = !isCollapsed" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-inverse-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/" target="_self" > CrossLinkMedia</a>
        </div>
        <div class="navbar-collapse collapse navbar-inverse-collapse" collapse="isCollapsed">
            
             
            <ul class="nav navbar-nav ng-cloak"> 
                <li ng-show="auth.isAuthenticated() && user().role === 'Operator'"> <a href="/chatroom" target="_self" ><i class="fa fa-wechat" > </i> Chatroom </a></li>  
                <!--<li ng-show="auth.isAuthenticated() && user().role === 'Administrator'"><a> <i> <span class="fa fa-phone"></span></i><strong>{{user().phoneNumber}} </strong></a></li>-->
                <!--<li ng-show="auth.isAuthenticated() && user().role === 'Administrator'"><a> <i> <span class="fa fa-key"></span></i><strong>{{user().APIkey}} </strong></a></li>-->
                <li ng-show="auth.isAuthenticated() && user().role === 'Administrator'"><a href="/management/operators" target="_self"> <i> <span class="fa fa-users"></span></i> Operators</a></li>
                
<!--                <li ng-show="auth.isAuthenticated() && user().role === 'Administrator'" class="dropdown">
                    
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <span class="fa fa-adn"></span> Administrator <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                       

                    </ul>
                </li>-->
                
                <!--<li ng-show="auth.isAuthenticated() && user().role === 'Administrator'"><a  ng-click ="getApiKey()" _href="/apikey" target="_self" > <span class="fa fa-key"></span> Get API Key</a></li>-->
            </ul> 
            <ul class="nav navbar-nav navbar-right"  ng-cloak>
            </ul>
            <ul class="nav navbar-nav navbar-right ng-cloak" >
                <li ng-hide="auth.isAuthenticated()"><a href="/login">Login</a></li>
                <li ng-hide="auth.isAuthenticated()"><a href="/register">Register</a></li>
                <!--<li ng-show="auth.isAuthenticated()"><p class="navbar-text">Hello, <strong><i class="fa fa-user"> </i> {{user().email}}</strong></p></li>-->
                <li ng-show="auth.isAuthenticated()&& user().role != 'Administrator'"><a href="#" ng-click="logout()"><i class="fa fa-sign-out"></i> Logout <strong>(<i class="fa fa-user"> </i> {{user().email}}</strong>)</a></li>

                <li class="dropdown" ng-show="auth.isAuthenticated()&& user().role === 'Administrator'">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <strong><i class="fa fa-user"> </i> {{user().email}}</strong><b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        <li ng-show="auth.isAuthenticated()&& user().role === 'Administrator'"><a href="/management/account/profile"  target="_self" ><i class="fa fa-cc"></i> My Account </a></li>
                        <li ng-show="auth.isAuthenticated()&& user().role === 'Administrator'"><a href="/management/account/settings"  target="_self" ><i class="fa fa-cc"></i> Settings </a></li>
                        <!--<li><a href="#">Something else here</a></li>-->
                        <li class="divider"></li>
                            <!--<strong>(<i class="fa fa-user"> </i> {{user().email}})</strong>-->
                         <li ng-show="auth.isAuthenticated()"><a href="#" ng-click="logout()"><i class="fa fa-sign-out"></i> Logout </a></li>

                    </ul>
                </li>
                
                
            </ul>
        </div>
    </nav>    
        <toaster-container toaster-options="{'position-class': 'toast-bottom-right'}"></toaster-container>
</header>